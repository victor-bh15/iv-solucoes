// ──────────────────────────────────────────────────────────────────────────
// OpenNext — adaptador do Next.js para Cloudflare Workers/Pages.
// Este arquivo é INERTE para a build/deploy atual da Vercel: só é lido pelo
// CLI `opennextjs-cloudflare`. Mantê-lo no repo NÃO afeta `next build`.
//
// Caminho A (sair do Vercel Hobby → Cloudflare, que permite uso comercial).
// Ainda NÃO ativado: ver CUTOVER-CLOUDFLARE.md. Não há cache R2 configurado
// porque o site é praticamente todo estático (SSG); a única rota dinâmica é
// /api/chat, que não usa ISR. Se um dia houver ISR/data cache, plugar o R2.
// ──────────────────────────────────────────────────────────────────────────
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // incrementalCache: r2IncrementalCache,  // habilitar só se usar ISR/SSG cache
});
